name: App Static Analysis

on:
  pull_request:
    paths:
      - 'app/**'
  push:
    branches:
      - '**'
    paths:
      - 'app/**'

permissions:
  contents: read

jobs:
  static-analysis:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: app
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: app/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Prettier format check
        run: |
          set -euo pipefail
          if ! npx prettier --check --ignore-unknown .; then
            echo ""
            echo "Files that are not properly formatted:"
            npx prettier --list-different --ignore-unknown .
            exit 1
          fi

      - name: ESLint static analysis
        run: npx eslint .
